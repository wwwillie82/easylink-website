export type Integration = { title:string; slug:string; shortDescription:string; order:number; status:'published'|'draft'; media:{path:string;alt:string;todo:string}; };
export const integrations: Integration[] = ['NAV Online Számla','Magyar bankok / PSD2 / Aggreg8','Hostware','Számlázz.hu','Billingo','Cégjelző'].map((title, index)=>({title,slug:title.toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/\s*\/\s*/g,'-').replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,''),shortDescription:`${title} integrációs irány az Easylink admin-kompatibilis content modelljében.`,order:index+1,status:'published',media:{path:`/assets/nati/integrations/${index+1}.webp`,alt:`${title} placeholder`,todo:'Későbbi integrációs vizuál/logó helye.'}}));
export const publishedIntegrations = integrations.filter((item)=>item.status==='published').sort((a,b)=>a.order-b.order);
