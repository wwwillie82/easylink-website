import {
  GOLDEN_CTA_KEY,
  blockFixedRole,
  blockHasRole,
  findRoleBlock,
  fixedPresentationRoles,
  legacyRoleByKey,
  withoutBlocks,
} from './block-role-contract.mjs';

export { GOLDEN_CTA_KEY, blockFixedRole, blockHasRole, findRoleBlock, fixedPresentationRoles, legacyRoleByKey, withoutBlocks };
