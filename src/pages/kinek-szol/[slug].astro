---
import BaseLayout from '@/layouts/BaseLayout.astro';
import Header from '@/components/Header.astro';
import PageHero from '@/components/PageHero.astro';
import ContentBlocks from '@/components/ContentBlocks.astro';
import RelatedLinks from '@/components/RelatedLinks.astro';
import CTASection from '@/components/CTASection.astro';
import Footer from '@/components/Footer.astro';
import { publishedAudiences } from '@/content/audiences';
import { getDetailPage } from '@/lib/content/provider';

export function getStaticPaths() {
  return publishedAudiences.map((item) => ({ params: { slug: item.slug }, props: { item } }));
}

const props = Astro.props;
const item = await getDetailPage('audience_detail', props.item.slug) ?? props.item;
const related = publishedAudiences
  .filter((entry) => entry.slug !== item.slug)
  .map((entry) => ({ title: entry.title, href: `/kinek-szol/${entry.slug}/` }));
---

<BaseLayout title={item.seoTitle} description={item.seoDescription}>
  <Header />
  <main>
    <PageHero eyebrow="Kinek szól?" title={item.heroTitle} description={item.heroDescription} variant="detail" asset={item.heroAsset || undefined} height={item.heroHeight} imageFit={item.heroImageFit} imagePositionX={item.heroImagePositionX} imagePositionY={item.heroImagePositionY} imagePositionMobileX={item.heroImagePositionMobileX} imagePositionMobileY={item.heroImagePositionMobileY} overlayStrength={item.heroOverlayStrength} imageScale={item.heroImageScale} />
    <ContentBlocks blocks={item.blocks} />
    <RelatedLinks links={related} />
    <CTASection />
  </main>
  <Footer />
</BaseLayout>
